{% extends 'base.html.twig' %}


{% block title %}2pac!{% endblock %}

 {% block body %}
     {% if messages|length >0 %}
     <div>
     {% for message in messages|slice(0, limit) %}
         <div>{{ message }}</div>
     {% endfor %}
     </div>
     {% else %}
         <div>il n'y a pas de messages en attente</div>

     {% endif %}

 {% endblock %}
