{% extends 'base.html.twig' %}

{% block title %}All posts{% endblock %}
{% block body %}

    <div class=" grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-4">


        {% if posts | length %}
            {% for post in posts %}
                {{ include('micro_post/_post.html.twig', {post:post}) }}
            {% endfor %}

        {% else %}
            <div>il n'y a pas de post en attente</div>

        {% endif %}
    </div>

{% endblock %}